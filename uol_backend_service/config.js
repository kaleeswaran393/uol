const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo 555 */
      host: "localhost",
      user: "root",
      password: "Lumen893$",
      database: "uol",
    },
    listPerPage: 10,
  };
  module.exports = config;